name: payfussion
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"


# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6
  flutter_screenutil:
  flutter_bloc:
  shared_preferences:
  flutter_otp_text_field:
  loading_animation_widget: ^1.3.0
  firebase_core:      # Initialize Firebase
  firebase_auth:      # User authentication
  cloud_firestore:    # Storing user data
  #  firebase_messaging: # Push notifications
  #  firebase_crashlytics: # Crash reporting
  #  firebase_analytics: # User analytics
  pin_code_fields:
  go_router:
  password_strength_indicator_plus:
  animations:
  equatable:
  currency_picker: ^2.0.21
  flutter_credit_card: ^4.1.0
  intl:
  permission_handler: ^12.0.0+1
  camera:
  wave:
  shimmer: ^3.0.0
  flutter_staggered_animations:
  image_picker:
  mobile_scanner:
  nfc_manager:
  local_auth:
  qr_flutter:
  flutter_svg: ^2.2.0
  avatar_glow: ^3.0.1
  pdf: ^3.11.3
  path_provider: ^2.1.5
  share_plus: ^11.0.0
  printing: ^5.13.4
  open_file: ^3.5.10
  cached_network_image: ^3.4.1
  firebase_storage: ^12.4.9
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  fl_chart: ^1.0.0
  get_it: ^8.0.3
  math_expressions: ^3.0.0
  dartz: ^0.10.1
  flutter_stripe: ^11.5.0
  flutter_dotenv: ^5.2.1
  http: ^1.4.0
  dio: ^5.8.0+1
  device_info_plus: ^11.5.0
  package_info_plus: ^8.3.0
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  dialog_flowtter: ^0.3.3
  flutter_launcher_icons: ^0.14.4
  animated_text_kit: ^4.2.3
  intl_phone_field: ^3.2.0
  uuid: ^4.5.1
  flutter_local_notifications: ^19.4.2
  timeago: ^3.7.1
  badges: ^3.1.2
  metaballs: ^1.5.0
  google_fonts: ^6.3.2
  icons_plus: ^5.0.0
  flutter_spinkit: ^5.2.2
  flip_card_swiper: ^1.0.0

dependency_overrides:
  http: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/logo.png"
  min_sdk_android: 23

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^3.0.0
  build_runner: ^2.5.4
  hive_generator: ^2.0.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  uses-material-design: true


  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/bottom_navigation_bar/
    - assets/images/setting/
    - assets/images/transaction/
    - assets/images/cards/
    - assets/images/
    - assets/images/home/
    - assets/images/paybill/
    - assets/images/otherBank/
    - assets/images/movies/
    - assets/images/paybill/mobile_recharge/
    - assets/images/paybill/electric_city/
    - assets/images/paybill/internet/
    - assets/images/paybill/gas_bill/
    - assets/images/paybill/rent_payment/
    - assets/images/paybill/dth_recharge/
    - assets/images/paybill/movies/
    - assets/images/paybill/postpaid_bill/
    - assets/temps/
    - assets/images/home/donation/
    - assets/icons/
    - assets/icons/setting_icons/
    - assets/icons/transaction_screen_icons/
    - .env
    - assets/pay-fussion-igqa.json
  #   - images/a_dot_ham.jpeg

  fonts:
    - family: Montserrat
      fonts:
        - asset: assets/fonts/Montserrat/Montserrat-Bold.ttf
          weight: 700
        - asset: assets/fonts/Montserrat/Montserrat-Regular.ttf
          weight: 400
        - asset: assets/fonts/Montserrat/Montserrat-Black.ttf
          weight: 500
        - asset: assets/fonts/Montserrat/Montserrat-Medium.ttf
          weight: 600

    - family: Poppins
      fonts:
        - asset: assets/fonts/poppins/Poppins-Regular.ttf
          weight: 400
        - asset: assets/fonts/poppins/Poppins-Bold.ttf
          weight: 700
        - asset: assets/fonts/poppins/Poppins-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/poppins/Poppins-Medium.ttf
          weight: 500
        - asset: assets/fonts/poppins/Poppins-Light.ttf
          weight: 300

    - family: Roboto
      fonts:
        - asset: assets/fonts/Roboto/Roboto-Regular.ttf
          weight: 400
        - asset: assets/fonts/Roboto/Roboto-Medium.ttf
          weight: 500