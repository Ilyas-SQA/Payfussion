import 'package:flutter/material.dart';

class ShowBillsScreen extends StatelessWidget {
  const ShowBillsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
